# =============================================================================
# Azure App Service Configuration for VCK Backend
# =============================================================================

# This file is used by Azure App Service to configure the application
# Upload this file to Azure App Service Configuration -> General settings -> Startup Command

# Using gunicorn as the process wrapper for better performance on Azure
# The command below starts uvicorn workers through gunicorn
web: gunicorn -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 src.main:app --workers 1 --timeout 120

# Alternative: Direct uvicorn command (if gunicorn is not needed)
# web: uvicorn src.main:app --host 0.0.0.0 --port 8000 --workers 1
